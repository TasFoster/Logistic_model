# Процесс разработки

Рабочий журнал проектирования **новой аналитической модели** (ветка `main`),
совместимой с имитационной моделью напарника (`core/simulator`).

## Как ведётся этот журнал (правила)

1. **Append-only.** Каждый новый этап анализа/решения добавляется как отдельный
   пронумерованный раздел `## Этап N — …` в конец файла.
2. **Прошлое не переписываем.** Рассуждения предыдущих этапов остаются как есть —
   это история мысли, а не финальная документация.
3. **Ошибки помечаем, а не стираем.** Если позже выясняется, что раннее суждение
   было неверным, в том месте (или в новом этапе со ссылкой) ставится пометка:
   > ⚠️ **ИСПРАВЛЕНИЕ (Этап N):** … в чём была ошибка и как правильно.
4. Даты — абсолютные. Числа — с единицами.

---

## Этап 1 — Полный анализ: симулятор + условие + критерии (2026-07-31)

Цель этапа: **до начала кодирования** разобрать (а) что должна уметь аналитика по
условию и критериям, (б) как устроен симулятор напарника и какой «контракт» надо
соблюсти, чтобы обе модели читали один граф и сверялись. Кодирования на этом этапе нет.

### 1.1. Условие задачи (PDF `doc-1783007964`, 8 стр.)

**Исходные данные (жёстко заданы):**

| Параметр | Значение |
|---|---|
| Входящий поток | **100 000 товаров/ч** |
| Тара | КТЯ 600×400×400 мм, в среднем **27 товаров** в КТЯ → ~**3 700 КТЯ/ч** |
| Масса | КТЯ ≤ 30 кг, товар ≤ 10 кг |
| Палета | 800×1200 мм, **20 КТЯ** на палете → **185 палет/ч** (задано в условии!) |
| Направлений | **400** |
| Nonsort | товары > 320 мм / по форме/хрупкости/штрихкоду — доля **не задана**, обосновать самим |
| Оборот тары | **80% КТЯ реюз, 20% в брак**, брак вывозится, восполняется новыми КТЯ |
| Сортировщик | производит 10 000 тов/ч, обслуживается **10 операторами** (1 чел = 1000 тов/ч); минимум 1000 тов/ч при ручной загрузке |
| Ручная сортировка | **250 товаров/ч на человека** по 20 направлениям |
| Двухстадийность | 400 напр. = 20×20; ручная схема потребовала бы **800 человек** |
| Отгрузка | **24 ворот**; машина = 32 палеты, но только **16 под КТЯ** (остальное роллкейджи); **погрузка 2 ч** ⇒ 8 палет/ч на ворота |
| Выход | те же 100 000 тов/ч, упакованные в КТЯ по направлениям, «одна коробка — одно направление» |
| Площадь | ориентир **20 000–24 000 м²**, меньше без потери работоспособности = выше балл |

**Обязательные процессы/потоки для модели** (это же — чек-лист «1 балл за операцию»
в критериях, Раздел 2):

1. Ввоз палет в буфер приёмки (185 палет/ч)
2. Вскрытие палет (снятие плёнки/ленты, съём КТЯ), транспортировка до сортировки — с **траекторией**
3. Вскрытие КТЯ, установка коробов у зоны загрузки сортировщика
4. Перекладка товаров в сортировочную систему
5. Обработка **nonsort** (возвратный/вспомогательный поток и его влияние)
6. Автосортировка по направлениям
7. Установка пустых КТЯ на сортировщик
8. Поток новых и **вторично используемых КТЯ** (реюз 80/20, брак, вывоз, новые)
9. Контроль заполняемости КТЯ (уплотнение, плотность)
10. Съём заполненных КТЯ, транспортировка на заклейку
11. Заклейка КТЯ
12. Транспортировка КТЯ в буфер отгрузки
13. Сортировка КТЯ по направлениям (на палете — КТЯ нескольких направлений)
14. Палетизация (установка КТЯ на палеты)
15. Отгрузка через 24 ворот
+ вся сопутствующая внутренняя логистика (траектории товаров/коробов, пустая тара, брак, новые короба, влияние на площадь).

**Важно для нас:** условие прямо требует, что **работа начинается с аналитической
модели**, она обязана быть в отчёте и служить основой для симуляции. Допускается сдача
«аналитика + отчёт без полной симуляции» — но тогда нужны план центра, все потоки,
**площади** и **расчёт производительности по каждой операции**. То есть аналитика —
не приложение к симуляции, а самостоятельный обязательный артефакт.

### 1.2. Критерии оценки (PDF `doc-1783007980`, 3 стр.)

| Раздел | Критерий | Баллы | Кого касается |
|---|---|---:|---|
| 1 | Ясность материалов | 0–5 | отчёт/презентация |
| 1 | Публичное выступление | 0–5 | презентация |
| 2 | Полнота процессов и потоков | 0–5 (1/операция) | **аналитика + симуляция** |
| 2 | Учёт исходных ограничений и логики | 0–10 | **аналитика + симуляция** |
| 2 | Связность модели как единой системы | 0–5 | **оба** |
| 3 | Способ моделирования (глубина) | 0–10 | симуляция выше, но аналитика — фундамент |
| 3 | **Инженерная реалистичность** (производительность, площади, маршруты, узкие места, загрузка) | 0–10 | **ядро аналитики** |
| 3 | Детализация визуализации (3D = высокий балл) | 0–10 | визуализация |
| 4 | Уровень автоматизации/роботизации | 0–10 | концепция |
| 4 | **Занимаемая площадь и компоновка** (20–24 тыс. м²) | 0–10 | **ядро аналитики** |
| 5 | **Отображение метрик** (по операции и системе, графики, интервалы 1мин/1ч/12ч/24ч) | 0–15 | симуляция + аналитика |
| 5 | Возможность менять параметры | 0–5 | оба |
| 5 | **Аналитические выводы и проблемные зоны** (узкие места, очереди, пределы) | 0–10 | **аналитика + симуляция** |
| 6 | Полнота и качество отчёта | 0–10 | отчёт (аналитика обязательна) |
| 6 | Воспроизводимость и проверяемость | 0–10 | весь репозиторий |

**Вывод:** аналитическая модель напрямую тянет ≥ 50 баллов (инженерная реалистичность,
площадь, узкие места, полнота процессов, ограничения, метрики по операциям, отчёт).
Самый весомый одиночный критерий — метрики (0–15). Площадь (0–10) — **отдельный
критерий, который симуляция сейчас не закрывает** (см. 1.5).

### 1.3. Допустимое ПО (PDF `doc-1783009018`, 2 стр.)

- Проприетарное: **AnyLogic** (DES), КОМПАС-3D, nanoCAD.
- Open-source DES: **SimPy, Salabim**, JaamSim, Ciw, DESMO-J, OR-Tools.
- База: **Python, NumPy/SciPy/pandas**, Git, Docker, Jupyter.
- Форматы результатов: **CSV, JSON** для данных/метрик; PDF для отчёта; MP4 для видео.

**Вывод:** аналитику пишем на **Python + NumPy** (разрешено, воспроизводимо на сервере
проверки). Никаких экзотических зависимостей. Проверка идёт на изолированном сервере ⇒
всё должно запускаться без интернета и с явными зависимостями.

### 1.4. Симулятор напарника (`core/simulator`) — архитектура

Стек: **Python 3.10+ / SimPy 4** (`requirements.txt`: `simpy>=4.0,<5.0`).

| Файл | Роль |
|---|---|
| `graph_loader.py` | читает JSON графа (2 схемы), нормализует к каноническому виду |
| `model.py` | ядро DES: `Node` (воркеры-процессы), `Rib` (ограниченный буфер), `SortingCenterModel`, источники (палеты, тара), пулы (погрузчики), отказы |
| `directions.py` | профиль 400 направлений (Zipf, подбор alpha под 20/80), группировка для 2 стадии |
| `metrics.py` | сбор метрик + выгрузка CSV `object,metric,value,unit`; агрегаты 1мин/1ч/12ч/24ч; поиск узкого места |
| `run.py` | CLI-запуск + текстовая сводка |
| `scenarios.py` | сценарии: пик ×1.5/×2, рост nonsort, отказы узлов |
| `viz.py` | 2D-схема цеха в самодостаточном HTML |
| `test_smoke.py` | 24 теста физической согласованности |
| `graph_contract.json` | эталон: одностадийная сортировка + оборот тары, реальные мощности (8 узлов) |
| `graph_2stage.json` | **основной граф**: полный центр, 33 узла, 52 ребра, 2 стадии 20×20, выходная сторона, погрузчики |

**Ключевые механики симуляции:**
- **Ребро = `simpy.Store(capacity=storage)`** — это же входная очередь приёмника. Полный буфер ⇒ `put` вешает источник ⇒ **блокировка** и каскад.
- **Узел = N воркеров-процессов.** Воркер собирает вход по `inputs {тип:кол-во}` (сборка), держит `service` секунд, выдаёт выход по `outputs`.
- Метрики времени воркеров: **работа + блокировка + голодание + отказ = 100%** (тест `test_time_accounting_sums_to_100`).
- **Погрузчики — не узлы**, а пул на рёбрах (`pool`, `batch`): копят партию, берут единицу из пула, едут туда-обратно.
- **Направления**: товар рождается с направлением на вскрытии и несёт его до упаковки («одна коробка — одно направление», накопители-ячейки, flush по таймауту).

### 1.5. КОНТРАКТ СОВМЕСТИМОСТИ (что аналитика обязана трактовать так же)

Это самое важное для нашей задачи. Симулятор через `graph_loader.normalize()` понимает
поля так (аналитика **обязана** понимать их идентично, иначе сверка бессмысленна):

| Поле графа | Смысл | Как считает аналитика |
|---|---|---|
| `effecive_ellements[].ef` | производительность единицы ресурса, **шт/ч = срабатываний/ч** | мощность узла = **Σ ef** (срабатываний/ч) |
| — время | `service = 3600/ef` (нужно только симуляции) | аналитике не нужно |
| `output {тип: N}`, N≥1 | детерминированное размножение (1 КТЯ→27 товаров+1 короб) | поток выхода типа = λ·N |
| `output {тип: доля}`, доля<1 | вероятностная развилка (0.95/0.05) | поток выхода типа = λ·доля |
| `input {тип: N}` | сборка: сколько нужно на 1 срабатывание (27 товаров + 1 короб) | λ = поток_главного_входа / N |
| `storage` на ребре | ёмкость буфера (для площади и рекомендаций) | в стац. режиме **не влияет на средние потоки** |
| узел `Storage` (нет ef) | мгновенный сток | поток = сумме входов, выхода нет |
| узел `source` (`emit_type`,`target_rib`,`low`) | машина восполнения тары | λ = дефицит: спрос приёмника − прочие поступления |
| `type_node: input_list` | алиас точки входа | стартовый узел, λ = 185 палет/ч |
| узлы/рёбра **списком** и `input_list`/`output_list` | актуальная схема контракта (TasFoster) | грузим наравне со словарной |

**Единицы ef — критично.** `ef` — это **срабатываний узла в час**, а не «товаров».
Проверено на эталоне:
- unKTU `ef=4000` = 4000 **КТЯ**/ч (на входе КТЯ), а не товаров.
- Pack `ef=1500`×4 = 6000 **КТЯ_full**/ч, хотя на входе 27 товаров на срабатывание.
- Sort1 `ef=10000`×11 = 110000 **товаров**/ч (на входе товар).

**Формат сверки метрик (`metrics.py`):** CSV со столбцами `object, metric, value, unit`.
По узлам: `throughput` (шт/ч), `utilization` (%), `blocked`, `starved`, `down`, `workers`.
По рёбрам: `queue_mean`, `queue_max`, `buffer_fill_max`. По системе: вход, выходы по типам.
**Аналитика должна выгружать метрики в этом же формате** — тогда сверка = скрипт-diff.

**Узкое место (симулятор):** узел с макс. (работа + блокировка). Аналитический аналог —
узел с макс. загрузкой (поток/мощность).

### 1.6. Что симулятор НЕ считает (зона ответственности аналитики)

Симулятор считает поведение во времени. Он **не** считает:
- **Площадь (м²)** — а это отдельный критерий 0–10 (ориентир 20–24 тыс. м²). Данных площади в графе нет: `scale: {width:100,height:100}` у всех узлов — заглушка, `pos` + `transport.unit_m=40` дают только расстояния для транспортных плеч.
- **Суммарный штат и парк техники** — по концепции считается суммированием по узлам; в графе есть число элементов (`effecive_ellements`), но связка «сортировщик ⇒ 10 операторов», «ручная сортировка 250/ч ⇒ N человек» — это аналитическая надстройка.
- **Размерение ресурсов** (сколько единиц купить под поток λ, округление вверх) — прямая задача аналитики из конспекта (раздел 4, «три вопроса»).
- **Максимальную пропускную способность** (λ, при которой первый узел упирается в 100%).
- **Стоимость/экономику** (в критериях явно нет, но полезно для отчёта).

Это и есть ниша новой аналитики: **балансы потоков → размерение ресурсов → штат/парк →
площадь → узкие места и запас**, всё мгновенно и без симуляции, в общем формате для сверки.

### 1.7. Инварианты, с которыми аналитика ОБЯЗАНА сойтись

Из `test_smoke.py` (это же — точки сверки):

| Инвариант | Значение |
|---|---|
| Баланс сортировки | nonsort ≈ 5%, годных 95% |
| Оборот тары | вскрыто пустых КТЯ ≈ 1:1 к вскрытым КТЯ |
| Развилка тары | 80% реюз / 20% брак |
| Баланс тары | реюз + новые ≈ спрос упаковки |
| Сборка упаковки | выход КТЯ ≈ вход товаров / 27 |
| Целевая мощность | сортировка ~100 000 тов/ч |
| Палетизация | 1 палета = 20 КТЯ |
| Отгрузка | 24 ворот, 8 палет/ч на ворота (16 палет за 2 ч) |
| Ручная сортировка | 250 тов/ч на человека |
| Возврат nonsort | ~50% назад в поток (на упаковку), ~50% в роллкейджи |

**Проверочная цепочка потоков на эталоне `graph_contract.json` (аналитика должна выдать это мгновенно):**
```
вход           100 000 товаров/ч = 185.2 палет/ч
Depaletizciya  185.2 палет/ч   / cap 250   = 74.1%  → 3 704 КТЯ/ч
unKTU          3 704 КТЯ/ч     / cap 4000  = 92.6%  → 100 000 товаров/ч + 3 704 короба/ч
Sort1          100 000 тов/ч   / cap 110000= 90.9%  → 95 000 годных + 5 000 nonsort
Pack           95 000/27 = 3 518 КТЯ_full  / cap 6000 = 58.6%  (спрос коробов 3 518/ч)
TareSplit      3 704 короба/ч  / cap 5000  = 74.1%  → 2 963 реюз + 741 брак
MashinaNovyhKTY дефицит = 3 518 − 2 963 = 555 коробов/ч  / cap 2000 = 27.8%
УЗКОЕ МЕСТО    unKTU (92.6%) — совпадает с выводом симуляции
```
> Заметка: симуляция даёт брак ~774 и новые ~618 (transient + source держит буфер на
> уровне `low`), аналитика — чистый стационар 741/555. Небольшое расхождение — норма и
> тема для отчёта (см. конспект, раздел 7: сверка честна при загрузках < 100%).

### 1.8. Открытые решения и варианты действий

Ниже — развилки, которые надо закрыть до кодирования. По каждой — рекомендация.

**Р1. Где живёт аналитика и точка входа.**
Ветка `main`, пакет `core/analitic/` (сейчас там только пустой `main.py`; нет `__init__.py`).
Запуск по аналогии с симулятором: `python -m core.analitic.run --graph …`.
→ *Рекомендация:* так и сделать, зеркалить CLI симулятора (`--graph/--out`).

**Р2. Загрузчик графа — переиспользовать или свой.** (важнейшая развилка)
Конспект прямо требует: «код чтения/валидации — **один, не дублировать**». Но
`import core.simulator.graph_loader` тянет `core/simulator/__init__.py` → `model.py` →
**`import simpy`**, т.е. аналитика получит зависимость от SimPy.
Варианты:
- (а) Импортировать `graph_loader` как есть — гарантированно одинаковый парсинг, но тянет simpy в аналитику.
- (б) Свой мини-загрузчик в `analitic/` — независимость (чистый stdlib), но риск расхождения парсинга.
- (в) **Вынести общий загрузчик в `core/` (или сделать `simulator/__init__.py` ленивым)**, чтобы обе модели импортировали один модуль без simpy. Архитектурно чисто и ровно по конспекту.
→ *Рекомендация:* **(в)** — но это правка в общем коде, нужна по конспекту «только по договорённости голосом» с напарником. Если правку сейчас не хотим трогать — временно **(б)** с явной пометкой TODO на объединение.

**Р3. Как решать циклы (оборот тары, возврат nonsort).** (наставник оставил открытым)
Потоки с циклами — система линейных уравнений.
- (а) `numpy.linalg.solve` — точное решение за один шаг, красиво для отчёта.
- (б) Итеративный проход до сходимости — проще отлаживать, естественно ложится на «source добирает дефицит».
→ *Рекомендация:* сделать **топологический проход + добор дефицита источниками** (обрабатывает эталон и 2-стадийный граф, где рёбра образуют DAG, а source впрыскивает тару отдельно), а `numpy.linalg.solve` добавить как **независимую проверку** того же баланса. Два способа, которые обязаны сойтись, — в духе всей затеи.

**Р4. С какого графа начинать.**
→ *Рекомендация:* сперва `graph_contract.json` (8 узлов, есть тара и nonsort, легко сверять руками по 1.7), затем `graph_2stage.json` (33 узла, 400 направлений, выходная сторона). На 2-стадийном добавится маршрутизация Product по нескольким рёбрам одного типа (`dest_group`) — потребует профиля направлений (можно переиспользовать `directions.py`, он без simpy).

**Р5. Модель площади (данных сейчас нет — пробел).**
Критерий 0–10, но в графе нет реальных площадей. Варианты источника площади:
- (а) добавить в схему графа поле площади на узел (`area_m2` или реальные `scale`) — правка контракта, согласовать;
- (б) считать площадь аналитикой из мощности/оборудования (число сортировщиков × типовая площадь установки + буферы «X минут потока» × габарит КТЯ/палеты + проходы) — не трогает контракт, но нужны нормативы (обосновать в отчёте);
- (в) гибрид: базовые габариты оборудования — константы в аналитике, буферы — из `storage` рёбер.
→ *Рекомендация:* **(б)/(в)** — площадь как расчёт (ровно по концепции «площадь — результат, а не вход»), нормативы вынести в один конфиг и обосновать.

**Р6. Модель штата/парка (надстройка над `effecive_ellements`).**
Нужны правила: сортировщик 10000/ч ⇒ 10 операторов; ручная сортировка ⇒ 250/ч на человека; автоматы ⇒ 0 операторов и т.п. В графе этого нет.
→ *Рекомендация:* таблица «тип узла/ресурса → операторы на единицу» в конфиге аналитики; штат = Σ по узлам. Обосновать числа из условия.

**Р7. Формат вывода и сверка.**
→ *Рекомендация:* выгружать `results/analytic_metrics.csv` в формате `object,metric,value,unit` (как у симулятора) + отдельный скрипт `core/validation/` строит таблицу «узел — аналитика — симуляция — расхождение %». Это закрывает критерий воспроизводимости и «аналитические выводы».

### 1.9. Предлагаемый план следующих этапов

1. **Этап 2 — Скелет аналитики:** загрузчик (Р2) + расчёт балансов на `graph_contract.json` (Р3, Р4), сверка чисел из 1.7 руками.
2. **Этап 3 — Размерение и загрузки:** мощность/загрузка/узкое место/max λ по всем узлам; вывод CSV (Р7).
3. **Этап 4 — Штат, парк, площадь** (Р5, Р6) — закрытие критериев «площадь» и «инженерная реалистичность».
4. **Этап 5 — 2-стадийный граф** (`graph_2stage.json`): маршрутизация по направлениям, выходная сторона, погрузчики.
5. **Этап 6 — Скрипт сверки** `core/validation/` аналитика↔симуляция, таблица расхождений для отчёта.

---
*Конец Этапа 1. Следующий этап дописывается ниже отдельным разделом.*

---

## Этап 2 — Скелет аналитики и балансы на эталонном графе (2026-07-31)

Цель: собрать каркас аналитики и получить корректные стационарные балансы на
`graph_contract.json`, сверив их с ручным расчётом из §1.7.

### 2.1. Принятые решения (из развилок §1.8)

- **Р1** — пакет `core/analitic/`, запуск `python -m core.analitic.run --graph …`.
- **Р2** — свой лёгкий загрузчик `loader.py` (без SimPy), зеркалит контракт
  `graph_loader.normalize`. TODO на объединение в общий модуль оставлен в шапке файла.
- **Р3** — топологический проход (алгоритм Кана) + добор дефицита узлами `source`.
  `numpy.linalg.solve` как независимую перекрёстную проверку — **отложил**: рёбра
  образуют DAG, цикл тары замыкается источником, одного прохода достаточно.
  Вернусь к нему на этапе сверки (Этап 6) как ко второму независимому способу.
- **Р4** — начал с `graph_contract.json`.

### 2.2. Созданные файлы

| Файл | Роль |
|---|---|
| `core/analitic/__init__.py` | экспорт `load_graph`, `solve` |
| `core/analitic/loader.py` | чтение графа обеих схем → канонический вид, без SimPy |
| `core/analitic/balance.py` | решатель балансов: `lam[узел]`, `flow[ребро]`, загрузка, узкое место, предельный вход |
| `core/analitic/run.py` | CLI-сводка в стиле симулятора |

### 2.3. Результат на `graph_contract.json` — сошёлся с §1.7 ТОЧНО

```
Узкое место: unKTU (92.6%)     Предельный вход: ~108 000 товаров/ч
Depaletizciya  185 / 250  = 74.1%      Pack           3519 / 6000 = 58.6%
unKTU         3704 / 4000 = 92.6% <=   TareSplit      3704 / 5000 = 74.1%
Sorting     100000 /110000= 90.9%      MashinaNovyhKTY 556 / 2000 = 27.8%
рёбра: реюз 2963, брак 741, дефицит(новая тара) 556 — всё как в §1.7
```
⇒ ручной расчёт §1.7 **подтверждён**, исправлений в Этап 1 не требуется. Числа
совпадают и с прогоном симулятора (README: unKTU 3704, Sort 100002, Pack 3517).

### 2.4. Smoke-прогон на `graph_2stage.json` (33 узла) — известное ограничение

Основная цепочка верна: Palletizaciya 181 палет/ч → Otgruzka24vorot 94% (≈ 11 машин/ч);
Pack 3611 (с учётом возврата nonsort 2500 тов/ч на упаковку); оборот тары сходится.

> ⚠️ **ОГРАНИЧЕНИЕ (не ошибка, а плановый недодел):** 20 секций 2-й стадии получают
> поток товаров **поровну** (4750/ч на секцию) вместо деления по объёму направлений
> (`dest_group` + профиль Парето). Из-за этого мелкие секции показывают ложную
> перегрузку 118.8%, а узкое место определяется неверно (`Sort2_g10` вместо реального
> `unKTU`/`Otgruzka`). **Числам 2-й стадии в `graph_2stage.json` пока доверять нельзя.**
> Эталон `graph_contract.json` корректен полностью. Исправление — Этап 5.
>
> ✅ **СНЯТО в Этапе 5 (2026-07-31):** поток теперь делится по доле объёма группы.

### 2.5. Что дальше

- **Этап 3** — вывод метрик в общий CSV `object,metric,value,unit` + размерение
  ресурсов (мин. число единиц под поток, округление вверх) — по §1.9.
- **Этап 4** — штат/парк/площадь (Р5, Р6).
- **Этап 5** — маршрутизация по направлениям на 2-стадийном графе (снимает ограничение 2.4).
- **Этап 6** — скрипт сверки аналитика↔симуляция + `numpy.linalg.solve` как контроль.

---
*Конец Этапа 2.*

---

## Этап 3 — Метрики в общий формат + размерение ресурсов (2026-07-31)

Цель: выгружать метрики в общий CSV `object,metric,value,unit` (контракт сверки с
симуляцией) и считать размерение ресурсов (сколько единиц нужно под поток).

### 3.1. Созданные/изменённые файлы

| Файл | Роль |
|---|---|
| `core/analitic/resources.py` | размерение: `needed = ceil(throughput / произв_единицы)`, сравнение «есть/надо» |
| `core/analitic/metrics.py` | сбор строк `object,metric,value,unit` + запись CSV |
| `core/analitic/run.py` | + колонки «единиц (есть/надо)», итог по штату, запись CSV |

### 3.2. Размерение на `graph_contract.json`

| узел | загрузка | единиц есть/надо |
|---|---|---|
| Depaletizciya | 74.1% | 1 / 1 |
| unKTU | 92.6% (узкое место) | 1 / 1 |
| Sorting | 90.9% | **11 / 10** (запас 1) |
| Pack | 58.6% | **4 / 3** (запас 1) |
| TareSplit | 74.1% | 1 / 1 |
| MashinaNovyhKTY | 27.8% | 1 / 1 |
| **ИТОГО** | | **19 / 17** единиц ресурса |

Вывод для отчёта: конфигурация эталона имеет небольшой запас на сортировке и упаковке;
критичный узел (вскрытие КТЯ) — без запаса. Это и есть ответ на вопрос «размерение»
из конспекта §4.

### 3.3. CSV — общий формат сверки

`results/analytic_metrics.csv`: по узлам `throughput`(шт/ч)+`utilization`(%) — **пересекаются
с метриками симулятора**, остальное (capacity, units_have/needed, потоки рёбер,
max_input, bottleneck) — аналитические, обогащают отчёт и не мешают сверке.

### 3.4. Две ошибки, найденные и исправленные в ходе этапа

1. **Выработка `source` считалась выходом системы.** `output:Box 556` — это тара,
   которую впрыскивает машина новых КТЯ и потребляет упаковка, а не выход центра.
   Исправлено: узлы `source` исключены из подсчёта выходов. Остался верный
   единственный выход `output:KTY_full 3519 шт/ч`.
2. **Переопределение входа (`--input-stream`) не обновляло `input_stream` в
   результате**, из-за чего `max_input` считался от номинала графа, а шапка печатала
   старый поток. Исправлено: эффективный вход = активность старта × товаров-на-палету;
   проверено — вход 108 000 → узкое место unKTU ровно 100%.

### 3.5. Что дальше

- **Этап 4** — штат/парк/площадь (Р5, Р6): надстройка «единица ресурса → операторы»
  и расчёт площади (критерии 0–10 + 0–10).
- **Этап 5** — маршрутизация по направлениям на 2-стадийном графе.
- **Этап 6** — скрипт сверки аналитика↔симуляция.

---
*Конец Этапа 3.*

---

## Этап 4 — Штат, парк техники, площадь (2026-07-31)

Цель: закрыть критерии «занимаемая площадь» (0–10) и часть «инженерной
реалистичности» (штат/парк). Решения (ответы на Р5/Р6): штат — по нормативам из
условия; площадь — расчётом из оборудования + буферы.

### 4.1. Ключевой принцип — [PDF] vs [ДОП]

Все нормативы вынесены в `core/analitic/norms.py`. Каждое число помечено:
- **[PDF]** — прямо из условия (загрузчик сортировщика 1000 тов/ч; ручная сортировка
  250 тов/ч; габариты палеты 0.96 м², КТЯ 0.24 м²);
- **[ДОП]** — проектное допущение там, где условие молчит (операторы обслуживания
  автоматов, площади установок, коэф. проходов 1.35, ячейка накопителя 1 м²).

Условие прямо разрешает обосновывать недостающие параметры самостоятельно —
поэтому [ДОП]-значения это ручки модели, обосновываются в отчёте. Менять в одном файле.

### 4.2. Файлы

| Файл | Роль |
|---|---|
| `core/analitic/norms.py` | все нормативы штата/площади с пометками [PDF]/[ДОП] |
| `core/analitic/facility.py` | штат, парк, площадь из балансов + нормативов; классификатор ролей узлов |
| `run.py`, `metrics.py` | сводка + строки `operators`/`area`/`staff_total`/`area_total`/`park:*` в CSV |

### 4.3. Модель штата (norms.py)

- Автосортировщик (Sort1): загрузчики = throughput / 1000  → **100 человек** [PDF].
- Ручная сортировка (RuchnayaSort): 1 человек на 250 тов/ч → **20 человек** [PDF].
- 2-я стадия — автоматическая, без загрузчиков [ДОП].
- Прочие узлы — операторы обслуживания на единицу оборудования [ДОП].

### 4.4. Модель площади

```
площадь = ( Σ площадь_оборудования(нужные единицы × норма роли)
          + Σ площадь_буферов(ёмкость ребра × габарит тары × штабелирование)
          + накопители упаковки (400 ячеек × 1 м²) ) × 1.35 (проходы)
```

### 4.5. Результаты

| Показатель | `graph_contract` (одностадийный, без выходной стороны) | `graph_2stage` (полный центр) |
|---|---:|---:|
| Штат | 108 чел | **163 чел** |
| Парк | 10 сортеров, 3 упак., … | 10 сортеров + 30 секций 2-й ст. + 6 погрузчиков + 23 ворот + … |
| Площадь оборудования | 5 260 м² | 11 070 м² |
| Площадь буферов | 360 м² | 1 440 м² |
| Накопители | 400 м² | 400 м² |
| **ИТОГО площадь** | 8 127 м² | **17 429 м²** |

**Выводы для отчёта:**
- **163 человека против 800 при полностью ручной схеме** (из условия) — сильная
  история про автоматизацию (критерий 0–10).
- **17 429 м² < ориентира 20 000–24 000 м²** — по критерию площади (0–10) меньшая
  площадь без потери работоспособности = выше балл.

> ⚠️ **ОГОВОРКА:** на `graph_2stage` парк 2-й стадии (`auto_sorter_stage2×30`) и площадь
> оборудования завышены из-за приближения «поток делится поровну» (см. ограничение 2.4):
> мелкие секции показывают потребность в 2 единицах там, где хватит 1. После Этапа 5
> (маршрутизация по направлениям) число секций и площадь снизятся. Штат от этого почти
> не зависит (2-я стадия без загрузчиков), поэтому 163 чел — устойчивое число.
>
> ✅ **СНЯТО в Этапе 5 (2026-07-31):** секций стало 20, площадь оборудования 11 070 → 9 870 м²,
> итог 17 429 → 15 809 м². Штат 163 не изменился, как и предполагалось.

### 4.6. Что дальше

- **Этап 5** — маршрутизация по направлениям (снимет оговорку 4.5 и ограничение 2.4).
- **Этап 6** — скрипт сверки аналитика↔симуляция (`core/validation/`).

---
*Конец Этапа 4.*

---

## Этап 5 — Маршрутизация по направлениям (2 стадии) (2026-07-31)

Цель: снять ограничение 2.4 — делить поток товаров между 20 секциями 2-й стадии
пропорционально объёму группы направлений, а не поровну.

### 5.1. Почему поровну — неверно

При балансировке «змейкой» группы НЕ равны по объёму: самое жирное направление
(~18 600 тов/ч) неделимо и целиком попадает в одну группу. Поэтому доля объёма
группы — от **2.9% до 20.4%**, и мощности секций в графе (4 000…25 000) сделаны
именно под эти доли.

### 5.2. Что сделано

- `core/analitic/directions.py` — **порт** математики профиля из симулятора (Zipf +
  подбор alpha под 20/80 + балансировка «змейкой»), без зависимостей. Нужны только
  `group_volume_shares()`.
- `balance.py` — при делении выхода одного типа между несколькими рёбрами с
  `dest_group` поток делится по доле объёма группы; иначе (одно ребро) — как раньше.

### 5.3. Результат на `graph_2stage.json`

| было (Этап 2, поровну) | стало (Этап 5, по объёму) |
|---|---|
| секции по 4750/ч, мелкие «перегружены» 118.8% | секции 19 422…2 734/ч, загрузка **66–80%** |
| ложное узкое место `Sort2_g10` | реальное **`Otgruzka24vorot` 94.0%** |
| предел ~84 000 тов/ч | предел **~106 300 тов/ч** |
| парк секций ×30, площадь 17 429 м² | парк секций **×20**, площадь **15 809 м²** |

Профиль направлений совпал с README симулятора: `alpha=1.08`, верхние 20% = 80.0%,
1-е ~18 575 тов/ч, последнее ~29 тов/ч. Это **сильное свидетельство идентичности**
моего порта и профиля напарника (формальную сверку — на Этапе 6).

Загрузки секций 66–80% совпадают с выводом симуляции («balanced 65–79%») — две
независимые модели сошлись и на 2-й стадии.

### 5.4. Регрессия

`graph_contract.json` не изменился (unKTU 92.6%, предел 108 000) — правка затрагивает
только узлы с несколькими рёбрами одного типа.

### 5.5. Что дальше

- **Этап 6** — скрипт сверки аналитика↔симуляция (`core/validation/`): единая таблица
  «узел — аналитика — симуляция — расхождение %» + формальная сверка профиля
  направлений (`group_volume_shares` аналитики vs симулятора).

---
*Конец Этапа 5.*
